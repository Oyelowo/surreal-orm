apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    linkerd.io/created-by: linkerd/helm %!s(<nil>)
  labels:
    app.kubernetes.io/managed-by: pulumi
    helm.sh/chart: linkerd-crds-1.4.0
    linkerd.io/control-plane-ns: linkerd
  name: authorizationpolicies.policy.linkerd.io
spec:
  group: policy.linkerd.io
  names:
    kind: AuthorizationPolicy
    plural: authorizationpolicies
    shortNames: []
    singular: authorizationpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: Authorizes clients to communicate with Linkerd-proxied server
              resources.
            properties:
              requiredAuthenticationRefs:
                description: RequiredAuthenticationRefs enumerates a set of required
                  authentications. ALL authentications must be satisfied for the authorization
                  to apply. If any of the referred objects cannot be found, the authorization
                  will be ignored.
                items:
                  properties:
                    group:
                      description: Group is the group of the referent. When empty,
                        the Kubernetes core API group is inferred."
                      maxLength: 253
                      pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    kind:
                      description: Kind is the kind of the referent.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                      type: string
                    name:
                      description: Name is the name of the referent.
                      maxLength: 253
                      minLength: 1
                      type: string
                    namespace:
                      description: Name is the name of the referent. When unspecified,
                        this authentication refers to the local namespace.
                      maxLength: 253
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              targetRef:
                description: TargetRef references a resource to which the authorization
                  policy applies.
                properties:
                  group:
                    description: Group is the group of the referent. When empty, the
                      Kubernetes core API group is inferred.
                    maxLength: 253
                    pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    description: Kind is the kind of the referent.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: Name is the name of the referent.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - targetRef
            - requiredAuthenticationRefs
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
