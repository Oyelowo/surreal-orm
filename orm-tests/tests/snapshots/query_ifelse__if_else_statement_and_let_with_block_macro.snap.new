---
source: orm-tests/tests/query_ifelse.rs
assertion_line: 78
expression: queries_1.to_raw().build()
---
{
LET $val = 7;

LET $oye_name = 'Oyelowo';

LET $select_space_ship = (SELECT * FROM space_ship ORDER BY name DESC);

RETURN IF $val > 5 { SELECT * FROM space_ship ORDER BY name DESC; } ELSE IF $oye_name = 'Oyelowo' { SELECT * FROM weapon ORDER BY strength DESC; } ELSE { LET $x = 2505; };
}
